import SwiftUI

struct RunMapView: View {
    var body: some View {
        VStack {
            Text("지도페이지")
                .font(.largeTitle)
                .bold()
        }
    }
}

struct RunMapView_Previews: PreviewProvider {
    static var previews: some View {
        RunMapView()
    }
}
